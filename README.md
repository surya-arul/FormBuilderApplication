# Dynamic Form Builder in Blazor & Asp.Net MVC

In this application, I have used Blazor and MVC framework to dynamically generate Html tags based on user inputs, store survey data to database and retrieving
