@if (Control.OptionData != null)
{
    <div class="form-group mb-4">
        <InputText class="form-control" @bind-Value="Control.OptionData[Index]" />
        <input type="button" @onclick="() => RemoveOption()" class="btn btn-link" value="Remove" />
    </div>
}